# Copy to .env.test. The test suite truncates every table it touches,
# so point this at a throwaway database, never at your seeded dev one.
DATABASE_URL=postgres://portal:portal@localhost:5432/portal_test
JWT_SECRET=test-secret-that-is-long-enough
JWT_EXPIRES_IN=1h
CLIENT_ORIGIN=http://localhost:5173
